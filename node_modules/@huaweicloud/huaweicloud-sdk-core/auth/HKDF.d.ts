/// <reference types="node" />
/**
 * HKDF是一种基于HMAC的密钥导出函数，分为提取和扩展两步，用于从短密钥材料生成更长的密钥
 */
export declare class HKDF {
    private static HMAC_SHA1;
    private static HMAC_SHA256;
    private static DERIVATION_KEY_LENGTH;
    private static HMAC_ALGORITHM;
    private static ALGORITHM_HASH_LENGTH;
    private static EXPAND_CEIL;
    static getDerKeySHA256(accessKey: string | undefined, secretKey: string | undefined, info: string): string | null;
    static toHex(data: Buffer): string;
    private static extract;
    private static expand;
    private static expandFirst;
    private static expandOnce;
    private static getHashLen;
    private static hmacSHA256;
    private static hexStringToBinary;
    private static stringToBinary;
}
//# sourceMappingURL=HKDF.d.ts.map