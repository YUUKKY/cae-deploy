"use strict";
/*
 * Copyright 2023 Huawei Technologies Co.,Ltd.
 *
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.HcClient = void 0;
const IHttpRequestBuilder_1 = require("./http/IHttpRequestBuilder");
const ExceptionUtil_1 = require("./exception/ExceptionUtil");
const logger_1 = require("./logger");
const SdkStreamResponse_1 = require("./SdkStreamResponse");
const DefaultHttpClient_1 = require("./http/DefaultHttpClient");
class HcClient {
    constructor(client, clientOptions) {
        this.proxyAgent = '';
        this.httpClient = client;
        // Logging
        this.logger = (0, logger_1.getLogger)(HcClient.loggerName, logger_1.LogLevel.INFO);
        this.logger.debug('initialized');
        this.clientOptions = clientOptions;
    }
    withEndpoints(endpoints) {
        this.endpoints = endpoints;
        return this;
    }
    withCredential(credential) {
        this.credential = credential;
        return this;
    }
    withRegion(region) {
        this.region = region;
        return this;
    }
    withHttpsAgent(proxyAgent) {
        this.proxyAgent = proxyAgent;
        return this;
    }
    overrideEndpoints(endpoint) {
        const client = new DefaultHttpClient_1.DefaultHttpClient(this.clientOptions, endpoint);
        return new HcClient(client, this.clientOptions).withCredential(this.credential).withEndpoints(endpoint);
    }
    async sendRequest(options) {
        this.logger.debug('send request');
        this.httpClient.credentials = this.credential;
        const request = await this.buildRequest(options);
        this.httpClient.httpRequest = request;
        try {
            const response = await this.httpClient.sendRequest(request);
            return this.extractResponse(response);
        }
        catch (error) {
            throw ExceptionUtil_1.ExceptionUtil.generalException(error);
        }
    }
    async buildRequest(options) {
        var _a;
        if (this.region) {
            this.credential = await this.credential.processAuthParams(this, this.region.id);
        }
        let url = `${options.url}`;
        Object.entries(options.pathParams).forEach(([key, value]) => {
            url = url.replace(`{${key}}`, value + '');
        });
        if (options.method === 'DELETE'
            && (options.data && (Object.keys(options.data).length <= 0 || options.data.length <= 0))) {
            delete options.data;
        }
        if ((_a = options.headers) === null || _a === void 0 ? void 0 : _a['Content-Type']) {
            delete options.headers['Content-Type'];
        }
        if (options.contentType) {
            if (!options.headers) {
                options.headers = {};
            }
            options.headers['content-type'] = options.contentType.toLowerCase().includes('application/json')
                ? 'application/json'
                : options.contentType;
        }
        const builder = new IHttpRequestBuilder_1.HttpRequestBuilder();
        let httpRequest = builder
            .withEndpoint(this.endpoints[0])
            .withUrl(url)
            .withHeaders(options.headers)
            .withMethod(options.method)
            .withPathParams(options.pathParams)
            .withData(options.data)
            .withQueryParams(options.queryParams)
            .build();
        httpRequest = await this.credential.processAuthRequest(httpRequest);
        if (options['responseHeaders']) {
            httpRequest['responseHeaders'] = options['responseHeaders'];
        }
        httpRequest.proxy = this.proxyAgent;
        if (options.axiosRequestConfig) {
            httpRequest.axiosRequestConfig = options.axiosRequestConfig;
        }
        return httpRequest;
    }
    extractResponse(result) {
        var _a;
        const headers = result.headers;
        const contentType = (_a = headers['content-type']) === null || _a === void 0 ? void 0 : _a.toLowerCase();
        if (/(application\/octet-stream|image|application\/zip)/.test(contentType)) {
            const streamRes = new SdkStreamResponse_1.SdkStreamResponse();
            streamRes.body = result.data;
            streamRes.httpStatusCode = result.statusCode;
            return streamRes;
        }
        const response = typeof result.data === 'object' ? result.data : {};
        response.httpStatusCode = result.statusCode;
        return response;
    }
}
exports.HcClient = HcClient;
HcClient.loggerName = 'HcClient';
//# sourceMappingURL=HcClient.js.map